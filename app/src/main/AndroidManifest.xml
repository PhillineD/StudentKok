<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.phill.studentcheff">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="Student chef"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activitys.StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activitys.VActivity" />
        <activity android:name=".Activitys.MealsActivity" />
        <activity android:name=".Activitys.RecipeActivity" />
        <activity android:name=".Activitys.HistoryActivity" />
        <activity android:name=".Activitys.MeatActivity" />
        <activity android:name=".Activitys.VegaActivity" />
    </application>

</manifest>